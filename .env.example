# Cloudflare account & project (used by `npm run deploy:pages`).
# Required only if you want non-interactive deploys (e.g. CI).
# Omit them locally and `wrangler` will OAuth on first use.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
